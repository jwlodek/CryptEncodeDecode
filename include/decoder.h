#include "common.h"

char* first_decoding(char* message, char* first_key);

char* second_decoding(char* message, char* second_key);

char* decode_message(char* message, char* first_key, char* second_key);